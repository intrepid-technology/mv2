// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import * as React from "react";

import { PlasmicDialogActions } from "./plasmic/market_v_2/PlasmicDialogActions";

function DialogActions_({closeDialog, onSubmit, ...props}, ref) {
  return (
    <PlasmicDialogActions
      root={{ ref }}
      {...props}
      cancelButton={{
        onClick: () => closeDialog()
      }}
      submitButton={{
        onClick: () => {
          try{
            onSubmit && onSubmit()
            closeDialog()
          }catch(err){
            console.log({err})
          }
        }
      }}
    />
  );
}

const DialogActions = React.forwardRef(DialogActions_);

export default DialogActions;
